import 'package:easy_localization/easy_localization.dart';
import 'package:flutter/material.dart';

class LocalizationApp {
  late final Locale locale;

  LocalizationApp(this.locale);

  CheckLanguageLocale(Locale locale) {}
}
